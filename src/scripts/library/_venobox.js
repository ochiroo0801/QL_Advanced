function venoBox() {
  $(document).ready(function () {
    $(".venobox").venobox({
      closeColor: "#fab733",
      arrowsColor: "#fab733",
      spinColor: "#fab733",
      spinner: "cube-grid",
      autoplay: true,
    });
  });
}

export default venoBox;
